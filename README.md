# Transferable Black-Box Adversarial Attacks on Vision-Language Models

## Project Overview

This project investigates transferable adversarial attacks on large Vision-Language Models (VLMs). The objective is to apply imperceptible perturbations to a facial image such that a generative multimodal model describes it as a predefined target object (e.g., "a tabby cat") while remaining visually indistinguishable to humans.

The attack operates in a black-box setting where internal model weights of the target system are inaccessible.

---
